import React from 'react'
import styles from '@styles/hero.module.css'

const Hero = () => {
  return (
    <div className={styles.hero}>
        <button className={styles.comicCloset}>
            Comic Closet
        </button>
    </div>
  )
}

export default Hero